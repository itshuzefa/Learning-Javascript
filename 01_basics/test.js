// Declare a variable named `age` and assign your age to it.
// What is the data type of `age`?

let age = 15;
console.log(typeof age);//output:number

// Create two variables: `firstName` and `lastName`.
// Combine them into a variable called `fullName` and print it.

let firstName = "huzefa";
let lastName = "shaikh";
let fullName = firstName + lastName;
console.log(fullName);

//What will be the output of the following code?
let a = 5;
let b = "5";
console.log(a == b);  // output:true
console.log(a === b); // output:false

// Convert the string "123" to a number and add 10 to it.
//let str = "123";
// Your code here

let str = "123";
let str2 = Number (str);
console.log(str2 + 10);

// What will be the Boolean value of each of these?
Boolean(0);
Boolean("hello");
Boolean("");
Boolean(null);

console.log(Boolean);

// Write code that takes a number in a variable.
// If the number is greater than 10, print "Big number".
// Otherwise, print "Small number".